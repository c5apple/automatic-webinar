import { WebinarNamePipe } from './webinar-name.pipe';

describe('WebinarNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WebinarNamePipe();
    expect(pipe).toBeTruthy();
  });
});
